<div class="col-md-4 col-md-offset-4"></div>
  <div class="text-center border bg-light border-light p-5 col-4">
    <p class="h3 mb-4 font-weight-bold">Doładowanie konta</p>
  <h7>Zostaniesz przekierowany do zewnętrznego serwisu. Po potwierdzeniu płatności środki pojawią się na Twoim konci
  </h2>
<%= form_with model: @transfer, target: :_blank, local: true do |form| %>
  <div class="form-group">
    <%= form.label :kwota %><br>
    <%= form.text_field :amount, class: 'form-control', required: true %>
    <%= form.hidden_field :user_id, value: current_user.id %>
    <%= form.hidden_field :confirmed, value: false %>
    <%= form.hidden_field :kind, value: "Doładowanie" %>
  </div> 
  <%= form.submit "Doładuj", class: 'btn btn-dark', formtarget: :_blank %>

  <%= link_to 'Lista transakcji', transfers_path, class: 'btn btn-info' %>
<% end %>
</div>