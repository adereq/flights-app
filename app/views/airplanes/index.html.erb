<h2>Samoloty</h2>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Id</th>
        <th scope="col">Model</th>
        <th scope="col">Miejsca kl.ekonomicznej</th>
        <th scope="col">Miejsca kl.biznesowej</th>
      </tr>
    </thead>
  <% @airplanes.each do |airplane| %>
    <tbody>
      <tr>
        <a href="/aiplanes/<%= airplane.id %>">
        <th scope="row"><%= airplane.id %></th>
        <td><%= airplane.model %></td>
        <td><%= airplane.economy_seats %></td>
        <td><%= airplane.business_seats %></td>
        <td><%= link_to 'Edytuj', edit_airplane_path(airplane.id) %>  <%= link_to 'Usuń', airplane_path(airplane.id), method: :delete, data: { confirm: 'Are you sure?' } %> </td>
      </a>
      </tr>
    </tbody>
    <% end %>
  </table>
  <div class="row">
  <%= link_to 'Dodaj samolot', new_airport_path, class: 'btn btn-light' %>
</div>