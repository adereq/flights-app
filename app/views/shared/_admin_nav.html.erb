<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <a class="navbar-brand" id="navbar" href="/admin/home">FlightApp Admin</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbar">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
        <li>
          <a class="nav-item nav-link <%= "active" if current_page?(admin_home_path) %>" href="/admin/home">Strona główna</a>
        </li>
        <% if current_user.airline_manager_role || current_user.superadmin_role %>
          <li>
            <a class="nav-item nav-link <%= "active" if current_page?(airports_path) %>" href="/admin/airports">Lotniska</a>
          </li>
        <% end %>
        <% if current_user.airline_manager_role || current_user.superadmin_role %>
          <li>
            <a class="nav-item nav-link <%= "active" if current_page?(flights_path) %>" href="/admin/flights">Loty</a>
          </li>
        <% end %>
        <% if current_user.airline_manager_role || current_user.superadmin_role %>
          <li>
            <a class="nav-item nav-link <%= "active" if current_page?(airplanes_path) %>" href="/admin/airplanes">Samoloty</a>
          </li>
        <% end %>
        <% if current_user.airline_manager_role || current_user.superadmin_role %>
          <li>
            <a class="nav-item nav-link <%= "active" if current_page?(bookings_path) %>" href="/admin/bookings">Rezerwacje</a>
          </li>
        <% end %>
        <% if current_user.superadmin_role %>
          <li>
            <a class="nav-item nav-link <%= "active" if current_page?(users_path) %>" href="/admin/users">Użytkownicy</a>
          </li>
        <% end %>
        <%= admin_login_helper %>
      </ul>

     </div>
     <% if (current_user.present?) %>
     <ul class="navbar-nav ml-auto text-right">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink-333" data-toggle="dropdown"
          aria-haspopup="true" aria-expanded="false">
          <i class="fas fa-user"></i>
        </a>
        <div class="dropdown-menu dropdown-menu-right dropdown-default">
          <a class="dropdown-item" href="#"><%= admin_login_helper %></a>
        </div>
      </li>
     </ul>
    <% end %>

  <div class="right nav-item navbar-nav mr-auto collapse navbar-collapse">
</nav>

